import React from 'react';
import styles from '../styles/main.scss';

const NotFound = () => (
  <main className={styles.main}>
    <h2>The page is not found (Error 404)</h2>
  </main>
);

export default NotFound;
